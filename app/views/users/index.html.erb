<p id="notice"><%= notice %></p>

<table>
  <% @users.each do |user| %>
  <tr>
    <td><%= user.username %></td>
    <td> <%= user.email %></td>
    <td> <%= link_to "View User", user %></td>
  </tr>
  <% end %>
</table>
<p>There are a total of <%#= @comments.count %> comments. <%#= link_to "Check em out...", user_comments_path %></p>