<p id="notice"><%= notice %></p>

<p>
  <strong>Username:</strong>
  <%= @user.username %>
</p>

<p>
  <strong>Email:</strong>
  <%= @user.email %>
</p>

<p>
  <table>
    <% @comments.each do |comment| %>
    <tr>
      <td><%= comment.description %></td>
      <td>| <em><small>Last updated <%= time_ago_in_words(comment.updated_at || comment.created_at) %> go</small></em></td>
    </tr>
     <% end %>

  </table>
</p>

<%= link_to 'Edit', edit_user_path(@user) %> |
<%= link_to 'Back', users_path %>
